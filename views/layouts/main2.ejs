<!DOCTYPE html>
<html lang="ko">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>
        <%= locals.title %>
    </title>
    <meta name="description" content="My first application using Node.js, Express and MongoDB">
    <link rel="stylesheet" href="/css/style.css">
    <style>
        .user-container {
            text-align: center;
            margin-top: 10px;
        }
        
        .user-container a {
            display: block;
            font-weight: bold;
            font-size: 16px;
            text-decoration: underline;
            /* 🔹 밑줄 추가 */
            color: inherit;
            /* 🔹 기존 글자색 유지 */
            margin-bottom: 5px;
        }
    </style>
</head>

<body>

    <div class="container">

        <header class="header">
            <a href="/" class="header-logo">MyStory</a>

            <nav class="header-nav">
                <ul>
                    <li>
                        <a href="/home">Home</a>
                    </li>
                    <li>
                        <a href="/about">About</a>
                    </li>
                    <li>
                        <a href="/admin">Posts</a>
                    </li>
                </ul>
            </nav>

            <div class="header-button">
                <% if (user) { %>
                    <div class="user-container">
                        <a href="/admin">
                            <%= user.username %>님, 반갑습니다!</a>
                        <a href="/logout" onclick="setTimeout(() => { location.reload(); }, 100);">로그아웃</a>
                    </div>
                    <% } else { %>
                        <a href="/admin">로그인</a>
                        <% } %>
            </div>
        </header>

        <main class="main">
            <%- body %>
        </main>
    </div>

</body>

</html>